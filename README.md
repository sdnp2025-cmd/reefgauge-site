# reefgauge.com

The ReefGauge pre-launch landing page, served by GitHub Pages at
https://reefgauge.com. One static `index.html` plus `assets/`; no build.

Edit, commit, push - the site updates within a minute. The `CNAME` file
binds the custom domain; leave it alone.
